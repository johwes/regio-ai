# Sensitive credentials — fill in values before applying.
# Do NOT commit the populated version to git.
# Apply manually: oc apply -f stage3/manifests/00-secret.yaml -n jwesterl
apiVersion: v1
kind: Secret
metadata:
  name: regio-ai-secrets
  namespace: jwesterl
type: Opaque
stringData:
  LITELLM_API_KEY: "REPLACE_WITH_YOUR_LITELLM_API_KEY"
